# 公式使用手册

本文档列出了 `vue-canvas-sheet` 的 WASM 公式计算引擎当前支持的所有运算符和函数及其用法示例。

## 1. 基础运算符

目前引擎支持以下基本算术运算符：

| 运算符 | 说明 | 示例 | 结果 |
| :--- | :--- | :--- | :--- |
| `+` | 加法 | `=1 + 2` | `3` |
| `-` | 减法 / 负号 | `=10 - 5` <br> `=-10` | `5` <br> `-10` |
| `*` | 乘法 | `=3 * 4` | `12` |
| `/` | 除法 | `=12 / 4` | `3` |

*(注：逻辑比较运算符如 `=`、`>` 在当前引擎底层会返回 `#OP!`，如需条件判断建议使用相应函数。)*

---

## 2. 聚合函数 (Aggregate)

这些函数通常接受单元格范围（如 `A1:B10`）或多个单独参数。

| 函数 | 说明 | 示例 |
| :--- | :--- | :--- |
| **SUM** | 求和 | `=SUM(A1:A10, 5)` |
| **AVERAGE** | 平均值 | `=AVERAGE(B1:B20)` |
| **MAX** | 最大值 | `=MAX(A1, B1, C1)` |
| **MIN** | 最小值 | `=MIN(A1:C10)` |
| **COUNT** | 计数（仅统计数字） | `=COUNT(A1:A10)` |
| **COUNTA** | 计数（统计传入的参数/非空单元格） | `=COUNTA(A1:A10)` |

---

## 3. 数学函数 (Math)

| 函数 | 说明 | 示例 | 结果 |
| :--- | :--- | :--- | :--- |
| **ROUND** | 四舍五入到指定小数位 | `=ROUND(3.14159, 2)` | `3.14` |
| **ABS** | 绝对值 | `=ABS(-5)` | `5` |
| **INT** | 向下取整 | `=INT(3.9)` | `3` |
| **SQRT** | 平方根 | `=SQRT(16)` | `4` |

---

## 4. 文本函数 (Text)

| 函数 | 说明 | 示例 | 结果 |
| :--- | :--- | :--- | :--- |
| **CONCAT** | 拼接字符串 | `=CONCAT("A", "B", "C")` | `"ABC"` |
| **LEFT** | 从左侧提取字符 | `=LEFT("Hello", 2)` | `"He"` |
| **RIGHT** | 从右侧提取字符 | `=RIGHT("Hello", 2)` | `"lo"` |
| **LEN** | 字符串长度 | `=LEN("Hello")` | `5` |
| **UPPER** | 转大写 | `=UPPER("hello")` | `"HELLO"` |
| **LOWER** | 转小写 | `=LOWER("HELLO")` | `"hello"` |

---

## 5. 逻辑函数 (Logic)

| 函数 | 说明 | 示例 |
| :--- | :--- | :--- |
| **IF** | 条件判断 | `=IF(1, "真", "假")` |
| **AND** | 逻辑与（全部为真） | `=AND(1, 1)` |
| **OR** | 逻辑或（任一为真） | `=OR(0, 1)` |
| **NOT** | 逻辑非 | `=NOT(0)` |

*(注：逻辑真/假在运算中 `0` 表示假，非 `0` 数字或非空字符串表示真。)*

---

## 6. 日期与时间函数 (Date)

WASM 引擎中日期在底层表示为自 `1899-12-30` 起的 Excel 序列号（天数）。

| 函数 | 说明 | 示例 |
| :--- | :--- | :--- |
| **TODAY** | 当前日期（去除时间的序列号） | `=TODAY()` |
| **NOW** | 当前时间（包含时间的序列号） | `=NOW()` |
| **YEAR** | 提取年份 | `=YEAR(TODAY())` |
| **MONTH** | 提取月份 | `=MONTH(TODAY())` |
| **DAY** | 提取天数 | `=DAY(TODAY())` |

---

## 7. 错误代码说明

公式计算出错时可能返回以下代码：

- `#DIV/0!` - 除以零
- `#VALUE!` - 缺少必需的参数或错误的数据类型
- `#NAME?` - 未知的函数名
- `#NUM!` - 公式或函数中包含无效数字值（如负数的平方根）
- `#OP!` - 不支持的运算符
- `#ERR: <详情>` - 其他内部错误
